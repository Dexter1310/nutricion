{% extends '/admin/index.html.twig' %}


{% block title %}PERSONAS{% endblock %}


    {% block admin %}
        {{ parent() }}
        <div class=" container">
            <a href="{{ path('new_user') }}"> Agregar persona</a>
            <ul>
                {% for item in users %}

                    <li class="m-4">{{ item.email }} - {{ item.roles[0] }}
                        <button class="btn btn-success">Evolución</button>
                        <button class="btn btn-warning">Editar</button>
                        <button class="btn btn-dark">DAR DE BAJA</button>
                        <button class="btn btn-danger">DOCUMENTOS COMPARTIDOS</button>
                        {% if item.active %}
                            Activo
                        {% else %}
                            Inactivo
                        {% endif %}
                    </li>
                {% endfor %}
            </ul>
        </div>
    {% endblock %}

